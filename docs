# make the docs folder and move files into it
mkdir docs && mv *.pdf *.docx docs/

# add everything inside docs to git
git add docs/

# commit the changes
git commit -m "Add docs folder with writing samples"

# push to GitHub
git push origin main
